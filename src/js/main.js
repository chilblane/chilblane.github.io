(function(window, document) {
    var $ = require('jquery');

    if( ~['complete', 'interactive'].indexOf(document.readyState)) {
        init();
    }

    function init() {
    }
})(window, document);
