.panel {
    display: flex;
    align-items: center;
    align-content: center;
    flex-wrap: wrap;
    min-height: 100vh;
    padding: 1px $leading 2 * $leading;
    &:nth-of-type(even) {
        background-color: #eee;
    }
}
