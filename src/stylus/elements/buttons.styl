.button,
button {
    display: inline-block;
    padding: 0.75em 1em;
    border-radius: 3px;
    font-weight: 700;
    text-align: center;
    transition-property: background-color, color;
    transition-timing-function: ease-out;
    transition-duration: 0.2s;
    &--small {
        font-size: 0.5 * $font-scale * 1rem;
    }
    &--link {
        background-color: $link-color;
        color: white;
        &:hover,
        &:focus {
            background-color: $link-color-hover;
            color: white;
        }
    }
    &--secondary {
        background-color: fade-out($color-bg-light, 70%);
        color: white;
        &:hover,
        &:focus {
            background-color: fade-out($color-bg-light, 40%);
            color: white;
        }
    }
    &--primary {
        background-color: darken($color-fire, 30%);
        color: white;
        &:hover,
        &:focus {
            background-color: darken($color-fire, 20%);
            color: white;
        }

    }
}
