.form {
    width: 100%;
    background-color: $color-bg-glass;
    padding: 1px $leading $leading;
    border-radius: 5px;
    fieldset {
        max-width: "calc(15em + %s)" % ($leading * 4);
        margin-left: auto;
        margin-right: auto;
    }
    &__control {
        margin-top: $leading;
    }
}

label {
    display: block;
    margin-bottom: 0.25 * $leading;
}

input[type='text'],
input[type='email'],
textarea {
    display: block;
    width: 100%;
    padding: 0.4em $leading * 0.5;
    font-family: $ff;
    font-size: inherit;
    background-color: $color-bg-glass;
    color: white;
    border: 1px solid transparent;
    &:focus {
        border-color: $color-bg-fade;
    }
}
